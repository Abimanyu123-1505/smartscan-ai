# Troubleshooting

- **Backend not starting**: Check if `PORT 8001` is already in use or if `data/` directory permissions are incorrect.
- **Low Accuracy**: Validate the dataset format. Ensure the SigMF data is not purely noise.
- **Frontend Connection Error**: Ensure `BACKEND_URL` is correctly configured in your environment or Docker network.
